@model IEnumerable<OnlineLibraryWebApplication.Models.Author>

@{
    ViewData["Title"] = "Список авторів";
}

<h1>Список авторів</h1>

@*<p>
    <a asp-action="Create">Додати нового автора</a>
</p>
 <table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Author1)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Author1)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Редагувати</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Детальніше</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Видалити</a>
            </td>
        </tr>
}
    </tbody>
</table> *@

@* <div class="list-group">
    @foreach (var author in Model)
    {
        <a href="@Url.Action("Index", "Books", new { Id = author.Id, Name = author.Author1, filteredBy = "authors" })" class="list-group-item list-group-item-action custom-card d-flex justify-content-between align-items-center">
            <h5 class="card-title">@Html.DisplayFor(modelItem => author.Author1)</h5>
            <span class="badge rounded-pill badge-dark-purple">@author.BookCount</span>
        </a>
    }
</div> *@
<div class="row row-cols-1 row-cols-md-3 g-4">
    @foreach (var author in Model)
    {
        <div class="col">
            <a href="@Url.Action("Index", "Books", new { Id = author.Id, Name = author.Author1, filteredBy = "authors" })" class="card custom-card-category">
                <img src="@Url.Content($"~/pictures/{author.Author1}.jpg")" class="card-img-top" alt="Картинка автора">
                <div class="card-body">
                    <h5 class="card-title">@Html.DisplayFor(modelItem => author.Author1)</h5>
                </div>
            </a>
        </div>
    }
</div>